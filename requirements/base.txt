# =============================================================================
# music-tag-web 后端 base 依赖 (Python 3.12 + Django 4.2 LTS)
# --------------------------------------------------------------------
# 注意：djangorestframework-jwt 已停止维护 (最后版本 2019)，已迁移到
# djangorestframework-simplejwt 5.x。
# `djangorestframework-jwt` 已被官方仓库 archive，
# 见 https://github.com/jazzband/djangorestframework-simplejwt
# =============================================================================

# --- Django core ---
Django==4.2.16            # LTS, supported until April 2026
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.6.0
django-filter==24.3
django-mysql==4.2.0
qqmusic-api-python>=0.6

# --- DB driver ---
PyMySQL==1.1.1            # 纯 Python，零 C 编译依赖

# --- Celery ---
celery==5.4.0
django-celery-beat==2.7.0
django-celery-results==2.5.1
redis==5.2.0              # redis-py 5

# --- WSGI / workers ---
gunicorn==23.0.0
gevent==24.2.1            # Python 3.12 wheel available

# --- Imaging / crypto ---
Pillow==10.4.0
pycryptodomex==3.20.0
demjson3>=3.0.6
Mako==1.3.6

# --- HTTP / parsing ---
requests==2.32.3
python-dateutil==2.9.0.post0
lxml==5.3.0

# --- Misc utilities used by services ---
tqdm==4.67.1
pathos==0.3.2
sqlalchemy==2.0.36        # bumped from 1.4 (modern API)
music-tag==0.4.3
